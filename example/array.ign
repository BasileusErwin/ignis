function main(): void {
	let array: int[] = [1, 2, 3];
	let arrayString: string[] = ["a", "b", "c"];

	for (let item in array) {
		println(item);
	}

	for (let item in arrayString) {
		println(item);
	}
}
